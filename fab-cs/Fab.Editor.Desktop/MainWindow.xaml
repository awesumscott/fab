<Window x:Class="Fab.Editor.Desktop.MainWindow"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:v="clr-namespace:Fab.Editor.Desktop.Views"
	xmlns:vm="clr-namespace:Fab.Editor.Core.ViewModels;assembly=Fab.Editor.Core"
	Title="{Binding Title}" Height="600" Width="900"
	d:DataContext="{d:DesignInstance vm:EditorMainWindowViewModel}"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	mc:Ignorable="d">
	<Window.InputBindings>
		<KeyBinding Key="Z" Modifiers="Ctrl" Command="{Binding UndoCommand}"/>
		<KeyBinding Key="Y" Modifiers="Ctrl" Command="{Binding RedoCommand}"/>
		<KeyBinding Key="Z" Modifiers="Ctrl+Shift" Command="{Binding RedoCommand}"/>
	</Window.InputBindings>
	<DockPanel>
		<StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="8">
			<Button Content="New Article" Command="{Binding NewArticleCommand}" Style="{StaticResource ToolbarButtonStyle}"/>
			<Button Content="Save" Command="{Binding SaveCommand}" Style="{StaticResource ToolbarButtonStyle}"/>
			<Button Content="Undo" Command="{Binding UndoCommand}" ToolTip="Ctrl+Z" Style="{StaticResource ToolbarButtonStyle}"/>
			<Button Content="Redo" Command="{Binding RedoCommand}" ToolTip="Ctrl+Y" Style="{StaticResource ToolbarButtonStyle}"/>
		</StackPanel>
		<StatusBar DockPanel.Dock="Bottom">
			<TextBlock Text="{Binding Status}"/>
		</StatusBar>
		<Grid>
			<Grid.ColumnDefinitions>
				<ColumnDefinition Width="220"/>
				<ColumnDefinition Width="Auto"/>
				<ColumnDefinition Width="*"/>
			</Grid.ColumnDefinitions>
			<ListBox Grid.Column="0"
				ItemsSource="{Binding Articles}"
				SelectedItem="{Binding SelectedArticle, Mode=TwoWay}"
				DisplayMemberPath="DisplayTitle"
				Margin="4"/>
			<GridSplitter Grid.Column="1" Width="4" HorizontalAlignment="Stretch" Background="LightGray"/>
			<ScrollViewer Grid.Column="2">
				<ContentPresenter Content="{Binding Editor}" Margin="8"/>
			</ScrollViewer>
		</Grid>
	</DockPanel>
</Window>
